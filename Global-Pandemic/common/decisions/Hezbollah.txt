HEZ_weekly_incomes_from_influence = {
}

HEZ_our_step_in_latin_america_decisions = {
	HEZ_sabotage_the_jewish_sectors_arg = {
		visible = {
			country_exists = ARG
			NOT = { is_ally_with = ARG }
			has_completed_focus = HEZ_Memory_of_AMIA
		}

		cost = 50

		days_re_enable = 100
		days_remove = 20
		available = { command_power > 20 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete HEZ_sabotage_the_jewish_sectors_arg"
			add_command_power = -30
			add_manpower = -20
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				arg_stab = ARG.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					ARG = {
						add_manpower = -50
						add_political_power = -25
						set_temp_variable = { treasury_change = -0.20 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						ARG = {
							add_manpower = 50
							add_political_power = 25
							set_temp_variable = { treasury_change = 0.20 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.23
							}
						}
					}
				}
				var:arg_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}

	}
	deals_with_drug_cartels_arg = {
		visible = {
			country_exists = ARG
			NOT = { is_ally_with = ARG }
			has_completed_focus = HEZ_New_Contracts_with_Drug_Cartels
		}

		cost = 50

		days_re_enable = 30
		days_remove = 30
		available = { command_power > 25 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision complete deals_with_drug_cartels_arg"
			add_command_power = -25
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				arg_stab = ARG.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					set_temp_variable = { treasury_change = 0.50 }
					modify_treasury_effect = yes
					add_equipment_to_stockpile = {
						type = Inf_equipment
						amount = -200
					}
					ARG = {
						add_political_power = -50
						set_temp_variable = { treasury_change = -0.50 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						ARG = {
							add_political_power = 50
							set_temp_variable = { treasury_change = 0.50 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.24
							}
						}
					}
				}
				var:arg_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	support_anti_zionist_parties_arg = {
		visible = {
			country_exists = ARG
			NOT = { is_ally_with = ARG }
			has_completed_focus = HEZ_Anti_Zionist_Parties
		}

		cost = 20

		days_re_enable = 50
		days_remove = 40

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision support_anti_zionist_parties_arg"
			set_temp_variable = { treasury_change = -0.35 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = { percent_change = 0.5 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = ARG }
			change_influence_percentage = yes

			ARG = {
				add_popularity = {
					ideology = communism
					popularity = 0.02
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	#Brazil
	HEZ_sabotage_the_jewish_sectors_bra = {
		visible = {
			country_exists = BRA
			NOT = { is_ally_with = BRA }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 50

		days_re_enable = 100
		days_remove = 20
		available = { command_power > 20 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_sabotage_the_jewish_sectors_bra"
			add_command_power = -30
			add_manpower = -20
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				bra_stab = BRA.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					BRA = {
						add_manpower = -50
						add_political_power = -25
						set_temp_variable = { treasury_change = -0.20 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						BRA = {
							add_manpower = 50
							add_political_power = 25
							set_temp_variable = { treasury_change = 0.20 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.23
							}
						}
					}
				}
				var:bra_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	deals_with_drug_cartels_bra = {
		visible = {
			country_exists = BRA
			NOT = { is_ally_with = BRA }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 50

		days_re_enable = 30
		days_remove = 30
		available = { command_power > 25 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision deals_with_drug_cartels_bra"
			add_command_power = -25
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				bra_stab = BRA.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					set_temp_variable = { treasury_change = 0.50 }
					modify_treasury_effect = yes
					add_equipment_to_stockpile = {
						type = Inf_equipment
						amount = -200
					}
					BRA = {
						add_political_power = -50
						set_temp_variable = { treasury_change = -0.50 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						BRA = {
							add_political_power = 50
							set_temp_variable = { treasury_change = 0.50 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.24
							}
						}
					}
				}
				var:bra_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	support_anti_zionist_parties_bra = {
		visible = {
			country_exists = BRA
			NOT = { is_ally_with = BRA }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 20

		days_re_enable = 50
		days_remove = 40

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision support_anti_zionist_parties_bra"
			set_temp_variable = { treasury_change = -0.35 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = { percent_change = 0.5 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_tBRAet = BRA }
			change_influence_percentage = yes

			BRA = {
				add_popularity = {
					ideology = communism
					popularity = 0.02
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	#Colombia
	HEZ_sabotage_the_jewish_sectors_col = {
		visible = {
			country_exists = COL
			NOT = { is_ally_with = COL }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 50

		days_re_enable = 100
		days_remove = 20
		available = { command_power > 20 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_sabotage_the_jewish_sectors_col"
			add_command_power = -30
			add_manpower = -20
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				col_stab = COL.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					COL = {
						add_manpower = -50
						add_political_power = -25
						set_temp_variable = { treasury_change = -0.20 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						COL = {
							add_manpower = 50
							add_political_power = 25
							set_temp_variable = { treasury_change = 0.20 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.23
							}
						}
					}
				}
				var:col_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	deals_with_drug_cartels_col = {
		visible = {
			country_exists = COL
			NOT = { is_ally_with = COL }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 50

		days_re_enable = 30
		days_remove = 30
		available = { command_power > 25 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision deals_with_drug_cartels_col"
			add_command_power = -25
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				col_stab = COL.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					set_temp_variable = { treasury_change = 0.50 }
					modify_treasury_effect = yes
					add_equipment_to_stockpile = {
						type = Inf_equipment
						amount = -200
					}
					COL = {
						add_political_power = -50
						set_temp_variable = { treasury_change = -0.50 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						COL = {
							add_political_power = 50
							set_temp_variable = { treasury_change = 0.50 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.24
							}
						}
					}
				}
				var:col_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	support_anti_zionist_parties_col = {
		visible = {
			country_exists = COL
			NOT = { is_ally_with = COL }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 20

		days_re_enable = 50
		days_remove = 40

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision support_anti_zionist_parties_col"
			set_temp_variable = { treasury_change = -0.35 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = { percent_change = 0.5 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_tCOLet = COL }
			change_influence_percentage = yes

			COL = {
				add_popularity = {
					ideology = communism
					popularity = 0.02
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	#Venezuela
	HEZ_sabotage_the_jewish_sectors_VEN = {
		visible = {
			country_exists = VEN
			NOT = { is_ally_with = VEN }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 50

		days_re_enable = 100
		days_remove = 20
		available = { command_power > 20 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_sabotage_the_jewish_sectors_VEN"
			add_command_power = -30
			add_manpower = -20
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				VEN_stab = VEN.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					VEN = {
						add_manpower = -50
						add_political_power = -25
						set_temp_variable = { treasury_change = -0.20 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						VEN = {
							add_manpower = 50
							add_political_power = 25
							set_temp_variable = { treasury_change = 0.20 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.23
							}
						}
					}
				}
				var:VEN_stab = {
 					#Mission Failed
				}
			}

		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	deals_with_drug_cartels_VEN = {
		visible = {
			country_exists = VEN
			NOT = { is_ally_with = VEN }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 50

		days_re_enable = 30
		days_remove = 30
		available = { command_power > 25 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision deals_with_drug_cartels_VEN"
			add_command_power = -25
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				VEN_stab = VEN.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					set_temp_variable = { treasury_change = 0.50 }
					modify_treasury_effect = yes
					add_equipment_to_stockpile = {
						type = Inf_equipment
						amount = -200
					}
					VEN = {
						add_political_power = -50
						set_temp_variable = { treasury_change = -0.50 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						VEN = {
							add_political_power = 50
							set_temp_variable = { treasury_change = 0.50 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.24
							}
						}
					}
				}
				var:VEN_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	support_anti_zionist_parties_VEN = {
		visible = {
			country_exists = VEN
			NOT = { is_ally_with = VEN }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 20

		days_re_enable = 50
		days_remove = 40

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision support_anti_zionist_parties_VEN"
			set_temp_variable = { treasury_change = -0.35 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = { percent_change = 0.5 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = VEN }
			change_influence_percentage = yes

			VEN = {
				add_popularity = {
					ideology = communism
					popularity = 0.02
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	#Mexico
	HEZ_sabotage_the_jewish_sectors_MEX = {
		visible = {
			country_exists = MEX
			NOT = { is_ally_with = MEX }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 50

		days_re_enable = 100
		days_remove = 20
		available = { command_power > 20 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_sabotage_the_jewish_sectors_MEX"
			add_command_power = -30
			add_manpower = -20
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				MEX_stab = MEX.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					MEX = {
						add_manpower = -50
						add_political_power = -25
						set_temp_variable = { treasury_change = -0.20 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						MEX = {
							add_manpower = 50
							add_political_power = 25
							set_temp_variable = { treasury_change = 0.20 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.23
							}
						}
					}
				}
				var:MEX_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	deals_with_drug_cartels_MEX = {
		visible = {
			country_exists = MEX
			NOT = { is_ally_with = MEX }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 50

		days_re_enable = 30
		days_remove = 30
		available = { command_power > 25 }
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision deals_with_drug_cartels_MEX"
			add_command_power = -25
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = {
				MEX_stab = MEX.stability
			}
			custom_effect_tooltip = HEZ_sabotage_the_jewish_sectors_info
			random_list = {
				0.6 = {
					set_temp_variable = { treasury_change = 0.50 }
					modify_treasury_effect = yes
					add_equipment_to_stockpile = {
						type = Inf_equipment
						amount = -200
					}
					MEX = {
						add_political_power = -50
						set_temp_variable = { treasury_change = -0.50 }
						modify_treasury_effect = yes
					}
					hidden_effect = {
						MEX = {
							add_political_power = 50
							set_temp_variable = { treasury_change = 0.50 }
							modify_treasury_effect = yes
							country_event = {
								id = hezbollah_events.24
							}
						}
					}
				}
				var:MEX_stab = {
 					#Mission Failed
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	support_anti_zionist_parties_MEX = {
		visible = {
			country_exists = MEX
			NOT = { is_ally_with = MEX }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 20

		days_re_enable = 50
		days_remove = 40

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision support_anti_zionist_parties_MEX"
			set_temp_variable = { treasury_change = -0.35 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = { percent_change = 0.5 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = MEX }
			change_influence_percentage = yes

			MEX = {
				add_popularity = {
					ideology = communism
					popularity = 0.02
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	destabilize_the_us = {
		visible = {
			country_exists = USA
			NOT = { is_ally_with = USA }
			has_completed_focus = HEZ_Our_Gift_To_the_Great_Satan
		}

		cost = 200

		days_re_enable = 70
		days_remove = 50

		available = {
			MEX = {
				influence_higher_20 = yes
			}
			COL = {
				influence_higher_20 = yes
			}
			VEN = {
				influence_higher_20 = yes
			}
			BRA = {
				influence_higher_20 = yes
			}
			ARG = {
				influence_higher_20 = yes
			}

			has_equipment = { Inf_equipment > 799 }
			has_equipment = { cnc_equipment > 299 }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision destabilize_the_us"
			add_equipment_to_stockpile = {
				type = Inf_equipment
				amount = -800
			}
			add_equipment_to_stockpile = {
				type = cnc_equipment
				amount = -300
			}
			set_temp_variable = { treasury_change = -5.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = { percent_change = 1 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = USA }
			change_influence_percentage = yes

			USA = {
				add_stability = -0.05
				add_timed_idea = {
					idea = HEZ_unknown_cartel_operations
					days = 60
				}
				hidden_effect = {
					add_stability = 0.05
					remove_ideas = HEZ_unknown_cartel_operation
					country_event = hezbollah_events.25
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 20
				threat > 0.09
			}
		}
	}
}
HEZ_assasination_of_salman_rushdie = {
	kill_rushdie = {
		visible = {
			has_completed_focus = HEZ_the_muslim_spring
		}

		fire_only_once = yes

		custom_cost_trigger = {
			command_power > 19
		}
		custom_cost_text = cp_cost_20

		available = {
			USA = {
				influence_higher_5 = yes
			}
			has_equipment = { Inf_equipment > 29 }
			has_equipment = { cnc_equipment > 9 }
			command_power > 19
		}

		complete_effect = {
 			log = "[GetDateText]: [Root.GetName]: Decision kill_rushdie"
			add_command_power = -20
		}
		remove_effect = {
			set_temp_variable = { treasury_change = -0.500 }
			modify_treasury_effect = yes
			if = {
				custom_effect_tooltip = rushdie_dies_tooltip
				limit = { has_dlc = "La Resistance" }
				set_temp_variable = {
					intel_over_us = civilian_intel@USA
				}
				random_list = {
					var:intel_over_us = {
						news_event = { id = hezbollah_events.39 }
					}
					15 = {
						news_event = { id = hezbollah_events.38 }
					}
				}
			}
			else = {
				custom_effect_tooltip = rushdie_dies_tooltip_nodlc
				if = {
					limit = { USA = { influence_higher_30 = yes } }
					random_list = {
						10 = {
							news_event = { id = hezbollah_events.39 }
						}
						1 = {
							news_event = { id = hezbollah_events.38 }
						}
					}
				}
				else_if = {
					limit = { USA = { influence_higher_20 = yes } }
					random_list = {
						10 = {
							news_event = { id = hezbollah_events.39 }
						}
						4 = {
							news_event = { id = hezbollah_events.38 }
						}
					}
				}
				else_if = {
					limit = { USA = { influence_higher_15 = yes } }
					random_list = {
						10 = {
							news_event = { id = hezbollah_events.39 }
						}
						6 = {
							news_event = { id = hezbollah_events.38 }
						}
					}
				}
				else_if = {
					limit = { USA = { influence_higher_10 = yes } }
					random_list = {
						10 = {
							news_event = { id = hezbollah_events.39 }
						}
						8 = {
							news_event = { id = hezbollah_events.38 }
						}
					}
				}
				else = {
					random_list = {
						1 = {
							news_event = { id = hezbollah_events.39 }
						}
						1 = {
							news_event = { id = hezbollah_events.38 }
						}
					}
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 100
				threat > 0.09
			}
		}
	}
}
HEZ_our_boots_on_africa_decisions = {
	HEZ_weapon_raidings_LBA = {
		visible = {
			country_exists = LBA
			NOT = { is_ally_with = LBA }
			has_completed_focus = HEZ_how_little_they_know
		}

		custom_cost_trigger = {
			command_power > 19
		}

		available = {
			LBA = { has_equipment = { Inf_equipment > 499 } }
			command_power > 100
		}

		custom_cost_text = cp_cost_20

		days_re_enable = 90
		days_remove = 30
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_weapon_raidings_LBA"
			add_command_power = -20
			set_temp_variable = { treasury_change = -0.75 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			LBA = {
				send_equipment = {
					equipment = Inf_equipment
					amount = 500
					target = HEZ
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_weapon_raidings_SUD = {
		visible = {
			country_exists = SUD
			NOT = { is_ally_with = SUD }
			has_completed_focus = HEZ_how_little_they_know
		}

		custom_cost_trigger = {
			command_power > 19
		}

		available = {
			SUD = { has_equipment = { Inf_equipment > 499 } }
			command_power > 100
		}

		custom_cost_text = cp_cost_20

		days_re_enable = 90
		days_remove = 30
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_weapon_raidings_SUD"
			add_command_power = -20
			set_temp_variable = { treasury_change = -0.75 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			SUD = {
				send_equipment = {
					equipment = Inf_equipment
					amount = 500
					target = HEZ
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_weapon_raidings_NIG = {
		visible = {
			country_exists = NIG
			NOT = { is_ally_with = NIG }
			has_completed_focus = HEZ_African_Hezbollah
		}

		custom_cost_trigger = {
			command_power > 19
		}

		available = {
			NIG = { has_equipment = { Inf_equipment > 499 } }
			command_power > 100
		}

		custom_cost_text = cp_cost_20

		days_re_enable = 90
		days_remove = 30
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_weapon_raidings_NIG"
			add_command_power = -20
			set_temp_variable = { treasury_change = -0.75 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			NIG = {
				send_equipment = {
					equipment = Inf_equipment
					amount = 500
					target = HEZ
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_weapon_raidings_EGY = {
		visible = {
			country_exists = EGY
			NOT = { is_ally_with = EGY }
			has_completed_focus = HEZ_how_little_they_know
		}

		custom_cost_trigger = {
			command_power > 19
		}

		available = {
			EGY = { has_equipment = { Inf_equipment > 499 } }
			command_power > 100
		}

		custom_cost_text = cp_cost_20

		days_re_enable = 90
		days_remove = 30
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_weapon_raidings_EGY"
			add_command_power = -20
			set_temp_variable = { treasury_change = -0.75 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			EGY = {
				send_equipment = {
					equipment = Inf_equipment
					amount = 500
					target = HEZ
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_weapon_raidings_CHA = {
		visible = {
			country_exists = CHA
			NOT = { is_ally_with = CHA }
 			has_completed_focus = HEZ_how_little_they_know
		}

		custom_cost_trigger = {
			command_power > 19
		}

		available = {
			CHA = { has_equipment = { Inf_equipment > 499 } }
			command_power > 100
		}

		custom_cost_text = cp_cost_20

		days_re_enable = 90
		days_remove = 30
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_weapon_raidings_CHA"
			add_command_power = -20
			set_temp_variable = { treasury_change = -0.75 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			CHA = {
				send_equipment = {
					equipment = Inf_equipment
					amount = 500
					target = HEZ
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	smuggle_weapons_from_south_sudanese_rebillions = {
		visible = {
			country_exists = SSU
			NOT = { is_ally_with = SSU }
			has_completed_focus = HEZ_how_little_they_know
		}

		custom_cost_trigger = {
			command_power > 59
		}

		available = {
			has_political_power > 20
		}

		custom_cost_text = cp_cost_60

		days_re_enable = 120
		days_remove = 40
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision smuggle_weapons_from_south_sudanese_rebillions"
			add_command_power = -20
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			add_equipment_to_stockpile = {
				type = Inf_equipment
				amount = 500
				producer = SSU
			}
			add_equipment_to_stockpile = {
				type = cnc_equipment
				amount = 100
				producer = SSU
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	smuggle_fuel_from_lybia = {
		visible = {
			country_exists = LBA
			NOT = { is_ally_with = LBA }
			has_completed_focus = HEZ_how_little_they_know
		}

		cost = 10

		days_re_enable = 120
 		days_remove = 20
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision smuggle_fuel_from_lybia"
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			add_fuel = 1000
			LBA = {
				set_temp_variable = { treasury_change = 1.00 }
				modify_treasury_effect = yes
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	smuggle_fuel_from_egypt = {
		visible = {
			country_exists = EGY
			NOT = { is_ally_with = EGY }
			has_completed_focus = HEZ_how_little_they_know
		}

		cost = 10

		days_re_enable = 120
		days_remove = 20
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision smuggle_fuel_from_egypt"
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			add_fuel = 1000
			EGY = {
				set_temp_variable = { treasury_change = 1.00 }
				modify_treasury_effect = yes
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	critsize_the_muslim_brotherhood_in_egypt = {
		visible = {
			country_exists = EGY
			NOT = { is_ally_with = EGY }
			has_completed_focus = HEZ_African_Hezbollah
		}

		cost = 60

		days_re_enable = 120
		days_remove = 60
		available = {
			has_political_power > 100
			EGY = {
				OR = {
					has_government = democratic
					neutrality_neutral_muslim_brotherhood_are_in_power = yes
				}
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision critsize_the_muslim_brotherhood_in_egypt"
			set_temp_variable = { treasury_change = -1.25 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			if = {
				limit = { EGY = { has_government = democratic } }
				EGY = { add_stability = -0.025 }
			}
			else_if = {
				limit = { EGY = { neutrality_neutral_muslim_brotherhood_are_in_power = yes } }
				EGY = {
					add_stability = -0.025
					add_war_support = -0.01
					set_temp_variable = { party_index = 12 }
					set_temp_variable = { party_popularity_increase = -0.02 }
					add_relative_party_popularity = yes
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	question_the_disappearance_of_musa_al_sadr_in_libyan_government = {
		visible = {
			country_exists = LBA
			NOT = { is_ally_with = LBA }
			has_completed_focus = HEZ_how_little_they_know
		}

		cost = 60

		days_re_enable = 120
		days_remove = 70
		available = {
			has_political_power > 100
			command_power > 9
			LBA = {
				influence_higher_5 = yes
			}
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision question_the_disappearance_of_musa_al_sadr_in_libyan_government"
			add_command_power = -10
			set_temp_variable = { treasury_change = -0.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			LBA = {
				set_party_index_to_ruling_party = yes
				set_temp_variable = { party_popularity_increase = -0.03 }
				add_relative_party_popularity = yes
				add_political_power = -20
				set_temp_variable = { percent_change = 2 }
				set_temp_variable = { tag_index = HEZ }
				set_temp_variable = { influence_target = LBA }
				change_influence_percentage = yes
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
}

HEZ_missions_in_the_middle_east_decisions = {
	HEZ_send_military_equipments_to_syria = {
		visible = {
			country_exists = SYR
			SYR = { has_civil_war = yes }
			has_completed_focus = HEZ_An_Inevitable_failure
		}

		custom_cost_trigger = {
			command_power > 9
		}
		custom_cost_text = cp_cost_10

		days_re_enable = 70
		days_remove = 5

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_send_military_equipments_to_syria"
			add_equipment_to_stockpile = {
				type = Inf_equipment
				amount = -250
			}
			add_equipment_to_stockpile = {
				type = cnc_equipment
				amount = -30
			}
			add_equipment_to_stockpile = {
				type = Anti_tank_0
				amount = -15
			}
		}
		remove_effect = {
			SYR = {
				add_equipment_to_stockpile = {
					type = Inf_equipment
					amount = 250
				}
				add_equipment_to_stockpile = {
					type = cnc_equipment
					amount = 30
				}
				add_equipment_to_stockpile = {
					type = Anti_tank_0
					amount = 15
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_volunteer_forces_for_syria = {
		visible = {
			country_exists = SYR
			SYR = { has_civil_war = yes }
			has_completed_focus = HEZ_An_Inevitable_failure
		}

		custom_cost_trigger = {
			command_power > 9
		}
		custom_cost_text = cp_cost_10

		days_re_enable = 70
		days_remove = 5

		available = {
			has_manpower > 3000
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_volunteer_forces_for_syria"
			add_war_support = -0.005
			add_manpower = -2500
		}
		remove_effect = {
			SYR = {
				add_war_support = 0.005
				add_manpower = 2500
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_our_advisors_in_damascus = {
		visible = {
			country_exists = SYR
			SYR = { has_civil_war = yes }
			has_completed_focus = HEZ_call_to_arms
		}

		cost = 20

		days_re_enable = 500
		days_remove = 10

		available = {
			command_power > 49
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_our_advisors_in_damascus"
			add_command_power = -50
		}
		remove_effect = {
			SYR = {
				add_command_power = 50
				add_ideas = UKR_foreign_advisors
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_weapons_from_iran = {
		visible = {
			is_ally_with = PER
			SYR = { has_civil_war = yes }
			has_completed_focus = HEZ_call_to_arms
		}

		cost = 20

		days_re_enable = 500
		days_remove = 10

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_weapons_from_iran"
			set_temp_variable = { treasury_change = -3.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			add_equipment_to_stockpile = {
				type = Inf_equipment
				amount = 300
				producer = PER
			}
			add_equipment_to_stockpile = {
				type = cnc_equipment
				amount = 50
				producer = PER
			}
			add_equipment_to_stockpile = {
				type = Anti_tank_0
				amount = 20
				producer = PER
			}
			set_temp_variable = { percent_change = 1 }
			set_temp_variable = { tag_index = PER }
			set_temp_variable = { influence_target = HEZ }
			change_influence_percentage = yes
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_our_funds_for_the_iraqi_people = {
		visible = {
			country_exists = IRQ
			NOT = { has_war_with = IRQ }
			has_completed_focus = HEZ_Our_Brothers_in_Sanaa_and_Baghdad
			NOT = { IRQ = { has_government = communism } }
		}

		cost = 30

		days_re_enable = 90
		days_remove = 15

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_our_funds_for_the_iraqi_people"
			set_temp_variable = { treasury_change = -2.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			set_temp_variable = { percent_change = 1.5 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = IRQ }
			change_influence_percentage = yes
			IRQ = {
				add_stability = -0.02
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_sabotage_wahhabi_schools = {
		visible = {
			country_exists = YEM
			NOT = { has_war_with = YEM }
			has_completed_focus = HEZ_and_Steal_the_Heathens
			NOT = { YEM = { has_government = communism } }
		}

		cost = 20

		days_re_enable = 90
		days_remove = 15
		available = {
			YEM = { influence_higher_5 = yes }
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_sabotage_wahhabi_schools"
			set_temp_variable = { treasury_change = -2.50 }
			modify_treasury_effect = yes
			add_command_power = -20
		}
		remove_effect = {
			YEM = {
				add_stability = -0.03
				add_popularity = {
					ideology = fascism
					popularity = -0.02
				}
				set_temp_variable = { party_index = 9 }
				set_temp_variable = { party_popularity_increase = 0.02 }
				add_relative_party_popularity = yes

			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_fund_protests_against_wahabbis_in_sanaa = {
		visible = {
			country_exists = YEM
			NOT = { has_war_with = YEM }
			has_completed_focus = HEZ_and_Steal_the_Heathens
			NOT = { YEM = { has_government = communism } }
		}

		cost = 20

		days_re_enable = 90
		days_remove = 15
		available = {
			YEM = { influence_higher_5 = yes }
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_fund_protests_against_wahabbis_in_sanaa"
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			YEM = {
				add_popularity = {
					ideology = fascism
					popularity = -0.03
				}
				set_temp_variable = { party_index = 9 }
				set_temp_variable = { party_popularity_increase = 0.03 }
				add_relative_party_popularity = yes
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_infrastructure_in_saadaa = {
		visible = {
			country_exists = YEM
			NOT = { has_war_with = YEM }
			has_completed_focus = HEZ_Invest_in_Saada
			NOT = { YEM = { has_government = communism } }
		}

		custom_cost_trigger = {
			check_variable = { treasury > 4.9 }
		}
		custom_cost_text = cost_5_0

		days_re_enable = 90
		days_remove = 15
		available = {
			YEM = { influence_higher_5 = yes }
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_infrastructure_in_saadaa"
			set_temp_variable = { treasury_change = -5.00 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			YEM = {
				951 = {
					add_building_construction = {
						type = infrastructure
						level = 1
						instant_build = yes
					}
				}
				set_temp_variable = { percent_change = 2 }
				set_temp_variable = { tag_index = HEZ }
				set_temp_variable = { influence_target = YEM }
				change_influence_percentage = yes
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_fund_houthi_propaganda = {
		visible = {
			country_exists = YEM
			NOT = { has_war_with = YEM }
			has_completed_focus = HEZ_Houthi_brothers
			NOT = { YEM = { has_government = communism } }
		}

		cost = 50

		days_re_enable = 90
		days_remove = 15
		available = {
			YEM = { influence_higher_5 = yes }
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_fund_houthi_propaganda"
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			YEM = {
				add_popularity = {
					ideology = communism
					popularity = 0.03
				}
				set_temp_variable = { party_index = 9 }
				set_temp_variable = { party_popularity_increase = 0.03 }
				add_relative_party_popularity = yes
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_shia_advisors_for_iraq = {
		visible = {
			country_exists = IRQ
			NOT = { has_war_with = IRQ }
			has_completed_focus = HEZ_Organize_Shii_Forces
			NOT = { IRQ = { has_government = communism } }
		}

		custom_cost_trigger = {
			command_power > 49
		}

		custom_cost_text = cp_cost_50

		days_re_enable = 90
		days_remove = 15
		available = {
			command_power > 80
			has_army_experience > 20
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_shia_advisors_for_iraq"
			add_manpower = -100
			army_experience = -5
		}
		remove_effect = {
			IRQ = {
				add_command_power = 40
				army_experience = 10
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_volunteers_for_iraq = {
		visible = {
			country_exists = IRQ
			NOT = { has_war_with = IRQ }
			has_completed_focus = HEZ_Organize_Shii_Forces
			NOT = { IRQ = { has_government = communism } }
		}

		custom_cost_trigger = {
			command_power > 19
		}

		custom_cost_text = cp_cost_20

		days_re_enable = 90
		days_remove = 15
		available = {
			has_manpower > 20000
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_volunteers_for_iraq"
			add_manpower = -1000
		}
		remove_effect = {
			IRQ = {
				add_manpower = 1000
			}
			set_temp_variable = { percent_change = 1 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = IRQ }
			change_influence_percentage = yes
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_fund_propaganda_for_badr = {
		visible = {
			country_exists = IRQ
			NOT = { has_war_with = IRQ }
			has_completed_focus = HEZ_Badr_in_the_shadows
			NOT = { IRQ = { has_government = communism } }
		}

		cost = 50

		days_re_enable = 90
		days_remove = 15
		available = {
			IRQ = { influence_higher_5 = yes }
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_fund_propaganda_for_badr"
			set_temp_variable = { treasury_change = -1.50 }
			modify_treasury_effect = yes
		}
		remove_effect = {
			IRQ = {
				add_popularity = {
					ideology = communism
					popularity = 0.03
				}
				set_temp_variable = { party_index = 9 }
				set_temp_variable = { party_popularity_increase = 0.03 }
				add_relative_party_popularity = yes
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_weapons_for_the_resistance = {
		visible = {
			country_exists = IRQ
			NOT = { has_war_with = IRQ }
			has_completed_focus = HEZ_Our_Brothers_in_Sanaa_and_Baghdad
			NOT = { IRQ = { has_government = communism } }
		}

		custom_cost_trigger = {
			check_variable = { treasury > 0.9 }
		}
		custom_cost_text = cost_1_0

		days_re_enable = 90
		days_remove = 15

		available = {
			has_equipment = { Inf_equipment > 149 }
			has_equipment = { cnc_equipment > 39 }
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_weapons_for_the_resistance"
			set_temp_variable = { treasury_change = -1.00 }
			modify_treasury_effect = yes
			add_equipment_to_stockpile = {
				type = Inf_equipment
				amount = -150
			}
			add_equipment_to_stockpile = {
				type = cnc_equipment
				amount = -40
			}
		}
		remove_effect = {
			set_temp_variable = { percent_change = 1 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = IRQ }
			change_influence_percentage = yes
			IRQ = {
				add_stability = -0.02
				add_war_support = -0.02
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_organize_raidings_in_yemen = {
		visible = {
			country_exists = YEM
			NOT = { has_war_with = YEM }
			has_completed_focus = HEZ_Our_Brothers_in_Sanaa_and_Baghdad
			NOT = { YEM = { has_government = communism } }
		}

		custom_cost_trigger = {
			command_power > 29
		}

		custom_cost_text = cp_cost_30

		days_re_enable = 90
		days_remove = 20

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_organize_raidings_in_yemen"
			add_command_power = -30
		}
		remove_effect = {
			YEM = {
				random_owned_state = {
					limit = { infrastructure > 0 }
					damage_building = {
						type = infrastructure
						damage = 1
					}
				}
			}
			set_temp_variable = { percent_change = 1.5 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = YEM }
			change_influence_percentage = yes
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_weapons_for_our_brothers = {
		visible = {
			country_exists = YEM
			NOT = { has_war_with = YEM }
			has_completed_focus = HEZ_Our_Brothers_in_Sanaa_and_Baghdad
			NOT = { YEM = { has_government = communism } }
		}

		custom_cost_trigger = {
			command_power > 9
		}

		custom_cost_text = cp_cost_10

		days_re_enable = 90
		days_remove = 15

		available = {
			has_equipment = { Inf_equipment > 249 }
			has_equipment = { cnc_equipment > 39 }
		}
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_weapons_for_our_brothers"
			add_command_power = -10
			add_equipment_to_stockpile = {
				type = Inf_equipment
				amount = -250
			}
			add_equipment_to_stockpile = {
				type = cnc_equipment
				amount = -40
			}
		}
		remove_effect = {
			set_temp_variable = { percent_change = 1 }
			set_temp_variable = { tag_index = HEZ }
			set_temp_variable = { influence_target = YEM }
			change_influence_percentage = yes
			YEM = {
				add_stability = -0.02
				add_war_support = -0.02
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_the_might_of_muqawimat = {
		visible = {
			country_exists = IRQ
			NOT = { has_war_with = IRQ }
			has_completed_focus = HEZ_Steal_the_Bastards
			IRQ = { has_government = democratic }
		}
		available = {
			has_equipment = { Inf_equipment > 299 }
			IRQ = {
				influence_higher_10 = yes
				custom_trigger_tooltip = {
					tooltip = HEZ_the_might_of_muqawimat_TT
					hidden_trigger = {
 						check_variable = { party_pop_array^9 > 0.29 }
					}
				}
			}
		}
		days_re_enable = 180
		days_remove = 20
		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_the_might_of_muqawimat"
			set_temp_variable = { treasury_change = -3.00 }
			modify_treasury_effect = yes
			add_command_power = -30
			add_equipment_to_stockpile = {
				type = Inf_equipment
				amount = -300
			}
			add_manpower = -100
		}
		remove_effect = {
			custom_effect_tooltip = HEZ_the_might_of_muqawimat_effect_TT
			USA = {
				country_event = { id = hezbollah_events.26 }
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
}

HEZ_border_skirmish_lebanon = {
	HEZ_huge_raid = {
		custom_cost_trigger = {
			command_power > 39
		}

		days_re_enable = 365

		custom_cost_text = cp_cost_30

		visible = {
			has_war = no
			country_exists = LEB
			has_completed_focus = HEZ_Skirmish_Raids
		}

		allowed = { tag = HEZ }

		available = {
			num_divisions > 12
			NOT = { has_country_flag = hezbollah_is_currently_doing_a_raid }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_huge_raid"
			set_country_flag = hezbollah_is_currently_doing_a_raid
			custom_effect_tooltip = HEZ_huge_raid_effect_tt
			set_country_flag = hezbollah_selected_huge_raid
			hidden_effect = {
				add_command_power = -40
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 201
						num_provinces = 1
						on_win = hezbollah_events.8
						on_lose = hezbollah_events.9
						on_cancel = hezbollah_events.22
					}

					defender = {
						state = 202
						num_provinces = 1
						on_win = hezbollah_events.10
						on_lose = hezbollah_events.11
						on_cancel = hezbollah_events.22
					}
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_medium_raid = {
		custom_cost_trigger = {
			command_power > 19
		}

		custom_cost_text = cp_cost_20

		days_re_enable = 365

		visible = {
			has_war = no
			country_exists = LEB
			has_completed_focus = HEZ_Skirmish_Raids
		}

		allowed = { tag = HEZ }

		available = {
			num_divisions > 6
			NOT = { has_country_flag = hezbollah_is_currently_doing_a_raid }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_medium_raid"
			set_country_flag = hezbollah_is_currently_doing_a_raid
			set_country_flag = hezbollah_selected_medium_raid
			custom_effect_tooltip = HEZ_medium_raid_effect_tt
			hidden_effect = {
				add_command_power = -20
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 201
						num_provinces = 1
						on_win = hezbollah_events.6
						on_lose = hezbollah_events.7
						on_cancel = hezbollah_events.22
					}

					defender = {
						state = 202
						num_provinces = 1
						on_win = hezbollah_events.10
						on_lose = hezbollah_events.11
						on_cancel = hezbollah_events.22
					}
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_small_raid = {
		custom_cost_trigger = {
			command_power > 9
		}

		days_re_enable = 365

		custom_cost_text = cp_cost_10

		visible = {
			has_war = no
			country_exists = LEB
			has_completed_focus = HEZ_Skirmish_Raids
		}

		allowed = { tag = HEZ }

		available = {
			num_divisions > 3
			NOT = { has_country_flag = hezbollah_is_currently_doing_a_raid }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_small_raid"
			set_country_flag = hezbollah_is_currently_doing_a_raid
			set_country_flag = hezbollah_selected_small_raid
			custom_effect_tooltip = HEZ_small_raid_effect_tt
			hidden_effect = {
				add_command_power = -10
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 201
						num_provinces = 1
						on_win = hezbollah_events.14
						on_lose = hezbollah_events.16
						on_cancel = hezbollah_events.22
					}

					defender = {
						state = 202
						num_provinces = 1
						on_win = hezbollah_events.10
						on_lose = hezbollah_events.11
						on_cancel = hezbollah_events.22
					}
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
}

HEZ_border_skirmish_israel = {
	HEZ_huge_raid_isr = {
		custom_cost_trigger = {
			command_power > 29
		}

		days_re_enable = 365

		custom_cost_text = cp_cost_30

		visible = {
			has_war = no
			country_exists = ISR
			has_completed_focus = HEZ_Turning_South
		}

		allowed = { tag = HEZ }

		available = {
			num_divisions > 12
			NOT = { has_country_flag = hezbollah_is_currently_doing_a_raid }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_huge_raid_isr"
			set_country_flag = hezbollah_is_currently_doing_a_raid
			custom_effect_tooltip = HEZ_huge_raid_effect_tt
			hidden_effect = {
				add_command_power = -30
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 201
						num_provinces = 1
						on_win = hezbollah_events.18
						on_lose = hezbollah_events.19
						on_cancel = hezbollah_events.21
					}

					defender = {
						state = 205
						num_provinces = 1
						on_win = hezbollah_events.12
						on_lose = hezbollah_events.13
						on_cancel = hezbollah_events.21
					}
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_medium_raid_isr = {
		custom_cost_trigger = {
			command_power > 19
		}

		days_re_enable = 365

		custom_cost_text = cp_cost_20

		visible = {
			has_war = no
			country_exists = ISR
			has_completed_focus = HEZ_Turning_South
		}

		allowed = { tag = HEZ }

		available = {
			num_divisions > 6
			NOT = { has_country_flag = hezbollah_is_currently_doing_a_raid }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_medium_raid_isr"
			set_country_flag = hezbollah_is_currently_doing_a_raid
			custom_effect_tooltip = HEZ_medium_raid_effect_tt
			hidden_effect = {
				add_command_power = -20
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 201
						num_provinces = 1
						on_win = hezbollah_events.17
						on_lose = hezbollah_events.20
						on_cancel = hezbollah_events.21
					}

					defender = {
						state = 205
						num_provinces = 1
						on_win = hezbollah_events.12
						on_lose = hezbollah_events.13
						on_cancel = hezbollah_events.21
					}
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
	HEZ_small_raid_isr = {
		custom_cost_trigger = {
			command_power > 9
		}

		days_re_enable = 365
		
		custom_cost_text = cp_cost_10

		visible = {
			has_war = no
			country_exists = ISR
			has_completed_focus = HEZ_Turning_South
		}

		allowed = { tag = HEZ }

		available = {
			num_divisions > 3
			NOT = { has_country_flag = hezbollah_is_currently_doing_a_raid }
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision HEZ_small_raid_isr"
			set_country_flag = hezbollah_is_currently_doing_a_raid
			custom_effect_tooltip = HEZ_small_raid_effect_tt
			hidden_effect = {
				add_command_power = -10
				start_border_war = {
					change_state_after_war = no
					attacker = {
						state = 201
						num_provinces = 1
						on_win = hezbollah_events.15
						on_lose = hezbollah_events.16
						on_cancel = hezbollah_events.21
					}

					defender = {
						state = 205
						num_provinces = 1
						on_win = hezbollah_events.12
						on_lose = hezbollah_events.13
						on_cancel = hezbollah_events.21
					}
				}
			}
		}
		ai_will_do = {
			base = 0
			modifier = {
				add = 1
				threat > 0.09
			}
		}
	}
}